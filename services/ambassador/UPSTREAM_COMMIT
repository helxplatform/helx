# Provenance for services/ambassador/chart.
#
# This chart was vendored from a SUBDIRECTORY of an upstream repo, so it is not
# a git subtree: it has no merge base and it is absent from SUBTREE_MAP.
# Sync it with:  make pull-vendored-chart NAME=ambassador
# Check it with: make check-vendored-sync
repo:      https://github.com/helxplatform/helx-chart
branch:    master
path:      charts/ambassador
# All upstream changes through this commit are incorporated here.
upstream:  264c8412f9991e9f0b86737c08a1f5b901196948
verified:  2026-09-21
# Byte-identical to upstream at that commit; no local divergence.
#
# `make check-vendored-sync` reports live whether this is behind upstream;
# that answer is not duplicated here, where it would go stale.
