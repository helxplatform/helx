{{- if .Values.ingress.create }}
{{- /*
The TLS Secret has its own certificate lifecycle and stays caller-managed, so
require it explicitly instead of rendering an empty secretName.
*/}}
{{- $tlsSecret := "" -}}
{{- if .Values.ingress.tls.enabled -}}
  {{- if .Values.SSL -}}
    {{- $tlsSecret = default "" .Values.SSL.nginxTLSSecret -}}
  {{- end -}}
  {{- if not $tlsSecret -}}
    {{- fail "resty: ingress.tls.enabled requires SSL.nginxTLSSecret to name an existing TLS Secret; create the Secret and set that value, or set ingress.tls.enabled=false" -}}
  {{- end -}}
{{- end -}}
{{- if .Values.basicAuth.enabled }}
  {{- $_ := set .Values.ingress.annotations "nginx.ingress.kubernetes.io/auth-type" "basic" }}
  {{- $_ := set .Values.ingress.annotations "nginx.ingress.kubernetes.io/auth-secret" (include "resty.basicAuthSecretName" .) }}
{{- end }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "resty.fullname" . }}
{{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
{{- end }}
spec:
{{- if .Values.ingress.ingressClassName }}
  ingressClassName: "{{ .Values.ingress.ingressClassName }}"
{{- end}}
{{- if .Values.ingress.tls.enabled }}
  tls:
  - hosts:
      - "{{ .Values.service.serverName }}"
    secretName: "{{ $tlsSecret }}"
{{- end}}
  rules:
  - host: "{{ .Values.service.serverName }}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "resty.fullname" . }}
            port:
              number: {{ .Values.service.httpPort }}
---
{{- if .Values.ingress.admin.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "resty.fullname" . }}-admin
  {{- with .Values.ingress.admin.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
{{- if .Values.ingress.ingressClassName }}
  ingressClassName: "{{ .Values.ingress.ingressClassName }}"
{{- end}}
{{- if .Values.ingress.tls.enabled }}
  tls:
  - hosts:
      - "{{ .Values.service.serverName }}"
    secretName: "{{ $tlsSecret }}"
{{- end}}
  rules:
  - host: "{{ .Values.service.serverName }}"
    http:
      paths:
      - path: /admin
        pathType: Prefix
        backend:
          service:
            name: {{ include "resty.fullname" . }}
            port:
              number: {{ .Values.service.httpPort }}
{{- end }}
{{- end }}
