# Git
.git
.gitignore

# Dev Container
.devcontainer/

# Helm chart
chart/

# nslcd sidecar builds as its own image (see .github/ci/images.yaml); keep it
# out of the webhook build context so sidecar changes don't alter this image.
nslcd-sidecar/

# Code that's not needed by the production build
tools/
tls-and-mwc/
Makefile

# Documentation/Config
CLAUDE.md
README.md
config.env

# Binary than gets built by Makefile
webhook-server/user-mutator
webhook-server/user_mutator
