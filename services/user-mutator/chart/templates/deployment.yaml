{{- $secretNames := include "user-mutator.effectiveSecretNames" . | fromYaml }}
{{/*
Roll the Pod when the webhook TLS material changes. The server reads its
certificate once at startup via http.ListenAndServeTLS, so a rotated Secret is
otherwise ignored until something restarts the process, and because
webhook.failurePolicy defaults to Ignore that stale cert fails every handshake
silently rather than surfacing an error.
*/}}
{{- $tlsChecksum := "" }}
{{- $persistedTls := lookup "v1" "Secret" .Release.Namespace (get $secretNames "cert") }}
{{- if $persistedTls }}
  {{- $tlsChecksum = default (dict) $persistedTls.data | toYaml | sha256sum }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "user-mutator.fullname" . }}
  labels:
    {{- include "user-mutator.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "user-mutator.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- if or .Values.podAnnotations $tlsChecksum }}
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with $tlsChecksum }}
        checksum/webhook-tls: {{ . }}
        {{- end }}
      {{- end }}
      labels:
        {{- include "user-mutator.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "user-mutator.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default (printf "v%s" .Chart.AppVersion) }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8443
              scheme: HTTPS
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8443
              scheme: HTTPS
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: config
              mountPath: /etc/user-mutator-config
            {{- range $mapName, $_ := .Values.config.maps }}
            - name: {{ printf "map-%s" $mapName | replace "_" "-" | lower }}
              mountPath: {{ printf "/etc/user-mutator-maps/%s" $mapName }}
              readOnly: true
            {{- end }}
            {{- range $key, $_ := $secretNames }}
            - name: {{ $key }}-secret
              mountPath: /etc/user-mutator-secrets/{{ $key }}
              readOnly: true
            {{- end }}
      volumes:
        - name: config
          configMap:
            name: {{ include "user-mutator.fullname" . }}-config
        {{- range $key, $mapName := .Values.config.maps }}
        - name: {{ printf "map-%s" $key | replace "_" "-" | lower }}
          configMap:
            name: {{ $mapName }}
            optional: true
        {{- end }}
        {{- range $key, $secretName := $secretNames }}
        - name: {{ $key }}-secret
          secret:
            secretName: {{ $secretName }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
